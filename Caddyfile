{
	# Optional: replace with your real domain to enable automatic HTTPS on :443
	# Example:
	# your-domain.com {
	# 	handle /api/* {
	# 		reverse_proxy backend:3000
	# 	}
	#
	# 	handle {
	# 		reverse_proxy frontend:8080
	# 	}
	# }

	# Default HTTP entrypoint for local usage on port 80
	http:// {
		handle /api/* {
			reverse_proxy backend:3000
		}

		handle {
			reverse_proxy frontend:8080
		}
	}
}
